{
  "name": "@puppo/mcp-server-test",
  "version": "0.0.1",
  "description": "A Model Context Protocol (MCP) server providing mathematical utility tools including Fibonacci sequence generation and number summation",
  "main": "index.js",
  "type": "module",
  "bin": {
    "mcp": "index.js"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "node --import tsx --test ./src/**/*.test.ts",
    "build": "tsc && chmod 755 build/index.js",
    "prepack": "cp README.md build/ 2>/dev/null || true && cp LICENSE build/ 2>/dev/null || true && cp package.json build/ 2>/dev/null || true",
    "publish:mcp": "npm run build && npm run prepack && cd build"
  },
  "files": [
    "build"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "mcp-server",
    "fibonacci",
    "math",
    "utilities"
  ],
  "author": "Luca Del Puppo <luca@delpuppo.net>",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^25.0.3",
    "eslint": "^9.39.2",
    "globals": "^17.0.0",
    "jiti": "^2.6.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.52.0"
  }
}
